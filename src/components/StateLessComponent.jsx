import React from "react";

const StateLessComponent = ({text}) => {
    return (
      <div>
        {text}
        this component dont have states
      </div>
    )
}

export default StateLessComponent